name = input("What is your name? ")

name = name.strip().title()   # Remove spaces at the beginning and at the end of the string: 
                              # Converts the first character of each word to upper case                                
print(f"Hello {name}")
